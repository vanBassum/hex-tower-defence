---
description: Reconstruct how the project came to understand something
---

Invoke the `reasoning-note` skill in retrieval mode. Read the notes in
`docs/reasoning/` (all of them if they fit), then answer the question below by
reconstructing how the understanding *evolved* — follow `builds-on` /
`supersedes` / `contradicts` chains and surface the assumptions that changed, not
just the latest state. Reading titles in order gives the evolution story. Tell me
how many notes you read.

$ARGUMENTS
